'use strict';

describe('Deployment: Rebalancing', () => {

  describe('BTCETH Rebalancing Manager', () => {

    /**
     * Check if the BTCETHERebalancingManager was deployed correctly with the following:
     * - Core
     * - wbtcMedianizerAddress
     * - wethMedianizerAddress
     * - wbtcAddress
     * - wethAddress
     * - SetTokenFactory
     * - LinearAuctionPriceCurve
     * - auctionTimeToPivot
     * - WBTC_MULTIPLIER
     * - WETH_MULTIPLIER
     */

  });

  describe('Rebalancing Set Token Factory', () => {

    /**
     * Check if there's a rebalanced set address with the correct params:
     * - SetTokenFactory
     * - rebalancingSetComponents
     * - rebalancingSetUnitShares
     * - rebalancingSetNaturalUnit
     * - rebalancingSetName
     * - rebalancingSetSymbol
     * - rebalancingSetCallData
     */

  });
  
});